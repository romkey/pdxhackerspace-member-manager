<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Email Preview: <%= @email_template.name %></title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      margin: 0;
      padding: 0;
      background: #f0f0f0;
    }
    .preview-header {
      background: #343a40;
      color: white;
      padding: 16px 24px;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .preview-header h1 {
      margin: 0 0 8px 0;
      font-size: 18px;
    }
    .preview-header .subject {
      background: rgba(255,255,255,0.1);
      padding: 8px 12px;
      border-radius: 4px;
      font-family: monospace;
    }
    .preview-tabs {
      background: #495057;
      padding: 0 24px;
      display: flex;
      gap: 4px;
    }
    .preview-tab {
      padding: 12px 24px;
      color: rgba(255,255,255,0.7);
      cursor: pointer;
      border: none;
      background: none;
      font-size: 14px;
    }
    .preview-tab:hover {
      color: white;
    }
    .preview-tab.active {
      color: white;
      background: #f0f0f0;
      border-radius: 4px 4px 0 0;
    }
    .preview-content {
      padding: 24px;
    }
    .preview-pane {
      display: none;
    }
    .preview-pane.active {
      display: block;
    }
    .html-preview {
      background: white;
      max-width: 700px;
      margin: 0 auto;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .text-preview {
      background: white;
      max-width: 700px;
      margin: 0 auto;
      padding: 24px;
      white-space: pre-wrap;
      font-family: monospace;
      font-size: 14px;
      line-height: 1.6;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .close-btn {
      position: absolute;
      top: 16px;
      right: 24px;
      background: rgba(255,255,255,0.2);
      border: none;
      color: white;
      padding: 8px 16px;
      border-radius: 4px;
      cursor: pointer;
    }
    .close-btn:hover {
      background: rgba(255,255,255,0.3);
    }
  </style>
</head>
<body>
  <div class="preview-header">
    <h1>Email Preview: <%= @email_template.name %></h1>
    <div class="subject"><strong>Subject:</strong> <%= @rendered[:subject] %></div>
    <button class="close-btn" onclick="window.close()">Close Preview</button>
  </div>

  <div class="preview-tabs">
    <button class="preview-tab active" data-tab="html">HTML Version</button>
    <button class="preview-tab" data-tab="text">Plain Text Version</button>
  </div>

  <div class="preview-content">
    <div id="html-pane" class="preview-pane active">
      <div class="html-preview">
        <%= render layout: 'mailer' do %>
          <%= @rendered[:body_html].html_safe %>
        <% end %>
      </div>
    </div>

    <div id="text-pane" class="preview-pane">
      <div class="text-preview"><%= @rendered[:body_text] %></div>
    </div>
  </div>

  <script>
    document.querySelectorAll('.preview-tab').forEach(function(tab) {
      tab.addEventListener('click', function() {
        document.querySelectorAll('.preview-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.preview-pane').forEach(p => p.classList.remove('active'));
        
        this.classList.add('active');
        document.getElementById(this.getAttribute('data-tab') + '-pane').classList.add('active');
      });
    });
  </script>
</body>
</html>
